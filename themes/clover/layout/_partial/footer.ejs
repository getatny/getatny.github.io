<footer id="footer" class="f-cf">
  <%= theme.mail %>
  <% if (theme.social) { %>
    <% _.forEach(theme.social, function(value, key) { %>
      <% if (value) { %>
        · <a href="<%= url_for(value) %>" target="_blank" class="nav-icn"><%= key %></a>
      <% } %>
    <% }); %>
  <% } %>
  <span class="copyright"><%= theme.copyright %></span>
</footer>

<%- js('highlight/pretty') %>
<%- css('highlight/tomorrow-night-eighties.min') %>
<script>
    document.addEventListener('DOMContentLoaded', (event) => {
        document.querySelectorAll('pre').forEach((pre) => {
            pre.classList.add('prettyprint', '<%- config.highlight.line_number ? 'linenums' : '' %>')
        });
        prettyPrint();
    });
</script>
